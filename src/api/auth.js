// Nativer Auth-Client — Teil der base44-Ablösung (Domäne Auth/User).
// Bündelt Login/Logout/Registrierung und aktuellen Benutzer (me, updateMe …).
import { auth } from './frontendClient.js';
export { auth };
export default auth;
